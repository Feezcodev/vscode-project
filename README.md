# VSCode Project

## Lab Steps Completed
1. **Repository Setup**: Initialized Git repository and pushed to GitHub.
2. **Feature Branch (Intro)**: Created `feature-add-intro` branch, added `intro.txt` with commit message "Add intro file", and merged into `main`.
3. **Collaboration Branch**: Created `feature-collab` branch, added `collab.txt` with commit message "Add collaboration updates", pushed to remote, and opened a Pull Request.
